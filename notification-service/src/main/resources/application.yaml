server:
  port: 8085

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:postgresql://localhost:5433/salon_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Firebase конфигурация
firebase:
  service-account:
    file: classpath:firebase-service-account.json

jwt:
  secret: 9a4f94d8170b1343feaa84181286e1112233445566778899aabbccddeeff0011

logging:
  level:
    com.service.salon: INFO