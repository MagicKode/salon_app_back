server:
  port: 8084
  address: 0.0.0.0

spring:
  application:
    name: history-service

  datasource:
    url: jdbc:postgresql://localhost:5433/salon_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # Только проверяет структуру, ничего не меняет в БД!
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Тот самый секретный ключ для расшифровки токена от auth-service
jwt:
  secret: 9a4f94d8170b1343feaa84181286e1112233445566778899aabbccddeeff0011